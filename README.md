# SS13 AI Backend (LLM)

Это бэкенд-сервис на Go для интеграции больших языковых моделей (LLM) через [Ollama](https://ollama.com/) в игру Space Station 13. Сервис предназначен для автоматической обработки и генерации ответов на внутриигровые факсы от имени Центрального Командования (Nanotrasen).

## ✨ Возможности

*   **Анализ факсов (`/fax/analyze`)**:
    *   Принимает содержимое факса (отправитель, заголовок, текст).
    *   Генерирует краткую выжимку (Summary).
    *   Определяет уровень срочности (Urgency: Low/Medium/High) на основе отправителя и ключевых слов.
    *   *Фишка:* Игнорирует "бред" от клоунов и ассистентов, но реагирует на угрозы от глав персонала.

*   **Генерация ответов (`/fax/reply`)**:
    *   Создает ответный факс в официальном бюрократическом стиле Nanotrasen.
    *   Поддерживает режимы:
        *   `approve`: Одобрение запроса ("Соответствует интересам корпорации...").
        *   `deny`: Отказ с бюрократической причиной (нет формы 27B-6, заморозка бюджета и т.д.).
        *   `custom`: Формализация произвольного ответа администратора в корпоративный стиль.

## 🛠 Требования

*   **Go** 1.25+
*   **Ollama** (запущенный локально или удаленно)
*   Модель для Ollama (по умолчанию используется `qwen3:4b-instruct-2507-q4_K_M`, но можно сменить через ENV).

## 🚀 Запуск

1.  **Запустите Ollama** и убедитесь, что нужная модель загружена:
    ```bash
    ollama run qwen3:4b-instruct-2507-q4_K_M
    ```

2.  **Клонируйте репозиторий**:
    ```bash
    git clone https://github.com/your-username/SS13-LLM-Ai.git
    cd SS13-LLM-Ai
    ```

3.  **Запустите сервер**:
    ```bash
    go run .
    ```
    Сервер запустится на порту `8000`.

## ⚙️ Конфигурация (Environment Variables)

Вы можете настроить параметры через переменные окружения:

| Переменная | Значение по умолчанию | Описание |
| :--- | :--- | :--- |
| `OLLAMA_URL` | `http://localhost:11434` | Адрес API Ollama |
| `OLLAMA_MODEL` | `qwen3:4b-instruct-2507-q4_K_M` | Имя используемой модели |
| `LISTEN_ADDR` | `:8000` | Адрес и порт для прослушивания |

## 🔌 API Endpoints

### 1. Анализ факса
**POST** `/fax/analyze`

**Request Body (JSON):**
```json
{
  "sender": "Captain Honk",
  "title": "Request for bananium",
  "content": "Please send more bananium to the station immediately."
}
```

**Response:**
```json
{
  "summary": "Капитан просит бананиум.",
  "urgency": "Low",
  "latency_ms": 1200
}
```

### 2. Ответ на факс
**POST** `/fax/reply`

**Request Body (JSON):**
```json
{
  "original_fax": {
    "sender": "Captain Honk",
    "title": "Request for bananium",
    "content": "Please send more bananium..."
  },
  "action": "deny",
  "custom_note": "" 
}
```
*   `action`: может быть `"approve"`, `"deny"`, или `"custom"`.
*   `custom_note`: используется только если action = `"custom"`.

**Response:**
```json
{
  "draft": "Центральное Командование рассмотрело ваш запрос. В поставке бананиума отказано из-за отсутствия формы 12-C. Слава НТ.",
  "latency_ms": 3500
}
```

## 📝 Лицензия

Этот проект создан для использования с серверами Space Station 13.
